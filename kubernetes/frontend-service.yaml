apiVersion: v1
kind: Service
metadata:
  name: taskmanager-frontend
spec:
  selector:
    app: taskmanager-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort  
